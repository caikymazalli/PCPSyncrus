// Import statements

// Other code before the changes

// Remove the mockData declaration

// Directly use tenant properties instead of mockData
const mrpEntries = tenant.mrpEntries;
const productionOrders = tenant.productionOrders;
const plants = tenant.plants;

// Use mrpEntries, productionOrders, and plants in the code as needed

// Other code after the changes